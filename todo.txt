-Add a tight "explain" button that uses coach to explain a move
-Allow coach to see workout routine
-BIG NEW COACH FEATURE - Add a button for coach to tweak the workout routine. It would expose the coach to the routine, tell it to critique it based on goals, readiness, etc. Then it can do two things:
--Be able to do a soft edit - Edit the routine without having to touch the backend, a manual edit of what the generator came up without
--Permanantly edit moves with, like the coach can currently do.
